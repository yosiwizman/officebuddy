# Environment variables for OfficeBuddy (do not commit .env.local)

# Required for agent operation
OPENAI_API_KEY=

# Default model used when a request omits `model`
# When set, the server should fallback to this value
OPENAI_MODEL=gpt-4o-mini
